## 1. Skill 文件结构与元数据

- [x] 1.1 在 `.cursor/skills/` 下创建 skill 目录（建议 `bug-autofix-loop/`）并添加 `SKILL.md`（含 YAML frontmatter：name/description）
- [x] 1.2 在 `SKILL.md` 中写入可发现的触发词清单（自动修复 bug、修 CI、修构建失败、修测试失败、修 lint、修崩溃等）
- [x] 1.3（可选）添加 `examples.md`：给出 build/test/lint/crash 四类输入示例与期望输出格式

## 2. 诊断-修复-验证闭环（核心流程）

- [x] 2.1 实现“证据收集”步骤：要求复现命令 + 首个根因错误段落/首个失败用例/异常栈
- [x] 2.2 实现“失败归类”决策：build/tests/lint/crash 四分支，并为每类列出默认验证命令
- [x] 2.3 实现 guided 交互：先输出根因判断 + 最小修复方案 + 验证命令，再执行改动
- [x] 2.4 实现迭代循环：验证失败则回到证据/归类；单次迭代只处理一个根因

## 3. 安全护栏与失败回退

- [x] 3.1 写入“最小改动优先 + 可回滚”原则与高风险改动前置说明（例如 Gradle/AGP/Kotlin 升级）
- [x] 3.2 写入“证据不足时停止盲修复”的回退行为：列出需要补充的信息与最小复现步骤
- [x] 3.3 写入“允许查外网”的使用边界：默认本地优先，仅在工具链/依赖兼容语义不清时查，并引用证据来源

## 4. 验证与试运行

- [x] 4.1 在本项目上用一个实际失败样例（或人造失败）演练：触发 skill → 诊断 → 修复 → 通过验证命令（若网络受限导致无法获取外部依赖/仓库，需改用本地样例或提供离线包）
- [x] 4.2 确认 Skill 文档不超过 500 行、术语一致、引用文件不超过一层深度（若有 reference/exmaples）
